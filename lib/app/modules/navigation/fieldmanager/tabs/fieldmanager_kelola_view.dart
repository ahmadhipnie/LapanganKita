import 'package:flutter/material.dart';

class FieldManagerKelolaView extends StatelessWidget {
  const FieldManagerKelolaView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Kelola'));
  }
}
